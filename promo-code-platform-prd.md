# promo-code MVP 开发 PRD

## 1. 产品定义

### 1.1 产品名称

`promo-code`

### 1.2 产品定位

一个极简的中文优惠码目录。商户注册并验证邮箱后，可以自行录入自己的优惠码；普通用户无需注册，直接浏览、复制优惠码并跳转到商户官网使用。

### 1.3 参考产品

参考页面：[TLD List Promo Codes](https://tld-list.com/promo-codes)

只参考以下内容：

- 顶部工具条和导航层级
- 深色视觉和紫红色主色
- 标题 Hero 区
- 高信息密度的优惠码表格
- 桌面端表格、移动端单列卡片

不复制参考站的品牌、Logo、商户数据、文案、代码或资源。

## 2. MVP 范围

### 2.1 必须实现

- 中文公开优惠码目录
- 优惠码搜索
- 优惠码复制
- 复制后打开对应商户官网
- 优惠码有效期展示
- 商户注册
- 商户邮箱验证
- 商户登录
- 商户自行创建商户资料
- 商户新增、编辑、暂停自己的优惠码
- 优惠码保存后立即上线，无人工审核
- 基础响应式布局

### 2.2 明确不实现

- 普通用户注册、登录、收藏、历史记录
- 商户人工审核流程
- 管理后台
- 分类页、商户详情页、优惠详情页
- 邮件订阅
- 广告位
- 联盟链接和佣金归因
- 自动采集和第三方优惠码 API
- 评论、评分、用户提交优惠码
- 多语言、货币切换、主题切换
- 支付、订单和真实购买转化追踪

## 3. 用户角色

| 角色 | 权限 |
| --- | --- |
| 普通用户 | 浏览、搜索、复制优惠码、打开商户官网 |
| 商户 | 注册账号、验证邮箱、创建商户资料、管理自己的优惠码 |

普通用户所有功能均可匿名使用，不保存个人账号数据。

## 4. 页面与路由

### 4.1 公开页面

| 路由 | 页面 | 说明 |
| --- | --- | --- |
| `/` | 优惠码目录 | 唯一的公开业务页面 |

### 4.2 商户页面

| 路由 | 页面 | 说明 |
| --- | --- | --- |
| `/merchant/register` | 商户注册 | 注册账号并创建商户资料 |
| `/merchant/login` | 商户登录 | 邮箱和密码登录 |
| `/merchant/verify` | 邮箱验证 | 验证注册邮箱 |
| `/merchant/deals` | 我的优惠码 | 新增、编辑、暂停自己的优惠码 |

不设置普通用户登录入口，不设置管理后台入口。

## 5. 视觉和布局规范

### 5.1 视觉基调

参考页面当前深色主题，使用以下基础色：

| 用途 | 色值 |
| --- | --- |
| 页面背景 | `#222222` |
| Hero 背景 | `#370120` |
| 主色/链接/按钮 | `#9e005d` |
| 主色 Hover | `#bd197b` |
| 主文字 | `#ffffff` |
| 次要文字 | `#b8b8b8` |
| 分割线 | `#5f5f5f` |
| 输入框背景 | `#2b2b2b` |

字体使用系统无衬线字体，优先 `Inter`、`-apple-system`、`BlinkMacSystemFont`、`Segoe UI`。

界面不提供语言、货币和深浅色切换，默认固定为中文深色界面。

### 5.2 页面层级

公开页面严格保持以下层级：

1. 顶部工具条
2. 主导航
3. 中间 Hero 标题区
4. 表格工具栏
5. 优惠码表格
6. 极简页脚

### 5.3 顶部工具条

桌面端高度约 `48px`，内容最大宽度约 `1100px`，右侧只显示：

- `商户登录`
- `商户注册`

不显示参考页面中的语言、货币、Dark 开关和其他工具。

移动端工具条保留商户入口，按钮触控区域不小于 `44px`。

### 5.4 主导航

桌面端内容最大宽度约 `1100px`，左侧显示品牌文字 `promo-code`，右侧显示：

- `优惠码`
- `商户发布`

当前页面的 `优惠码` 使用主色标识。

移动端主导航允许换行或使用紧凑布局，不使用复杂抽屉菜单。

### 5.5 Hero 区

Hero 为全宽深紫色区域，不使用图片、渐变或营销卡片。

内容居中显示：

- H1：`优惠码`
- 描述：`查找并使用最新的商户优惠码`

桌面端 H1 约 `48px`，移动端约 `36px`。Hero 上下内边距约 `48px`，移动端约 `36px`。

### 5.6 表格工具栏

表格上方显示：

- 搜索输入框：`搜索商户或优惠码`
- 排序选择：
  - `推荐`
  - `最新添加`
  - `即将结束`
- 结果数量

不设置分类筛选、评分筛选、广告筛选和复杂筛选面板。

搜索输入使用约 `300ms` 防抖，搜索条件写入 URL 的 `q` 参数。

### 5.7 优惠码表格

桌面端使用高密度五列表格，最大宽度约 `1100px`：

| 列 | 内容 |
| --- | --- |
| 优惠码 | 可点击复制的真实优惠码 |
| 商户 | 商户名称和官网域名 |
| 优惠内容 | 商户提交的简短优惠描述 |
| 截止时间 | 日期或“长期有效” |
| 使用限制 | 简短的使用条件 |

表格要求：

- 表头与内容左对齐
- 行高约 `58-64px`
- 行底部使用分割线
- Hover 时使用低透明度主色背景
- 优惠码使用主色并加粗
- 不使用大卡片堆叠
- 不出现横向滚动条

移动端每条优惠转为单列卡片，但保留相同信息顺序：

1. 商户
2. 优惠内容
3. 优惠码
4. 截止时间
5. 使用限制

### 5.8 页脚

页脚只显示：

`promo-code · 商户发布优惠码`

不显示博客、广告、联盟、订阅和其他产品入口。

## 6. 公开用户流程

### 6.1 浏览优惠码

1. 用户打开 `/`
2. 查看当前有效优惠码
3. 使用搜索框查找商户或优惠码
4. 使用排序选择调整列表

默认只展示：

- `published` 状态
- 当前时间已经开始
- 未超过结束时间的优惠码

### 6.2 复制并跳转

用户点击优惠码后：

1. 将优惠码写入剪贴板
2. 显示“优惠码已复制”
3. 在新标签页打开对应商户官网

商户官网地址来自商户资料，不允许普通用户修改，也不允许优惠码提交任意外部跳转地址。

复制失败时：

- 显示优惠码文本
- 提示用户手动复制
- 不阻塞打开商户官网

### 6.3 空状态

搜索无结果时显示：

- `没有找到匹配的优惠码`
- `清除搜索`

不引导用户注册，不提供用户提交入口。

## 7. 商户注册和发布

### 7.1 注册字段

商户注册只需要以下字段：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| 邮箱 | 是 | 用于登录和邮箱验证 |
| 密码 | 是 | 最少 8 位 |
| 商户名称 | 是 | 展示在公开列表 |
| 官网地址 | 是 | 用户跳转地址，必须为 HTTPS |

注册后创建：

- 一个商户账号
- 一个商户资料
- 一个邮箱验证状态

未验证的商户资料保持 `pending_verification`，不出现在公开列表。

### 7.2 邮箱验证

- 注册后发送验证邮件
- 验证链接一次性使用
- 验证链接有效期 24 小时
- 支持重新发送验证邮件
- 验证成功后账号和商户资料变为 `active`
- 未验证账号不能登录商户管理页，或登录后只能看到验证提示

MVP 开发预览可使用页面内的模拟验证入口；生产环境接入邮件服务后替换。

### 7.3 商户登录

商户使用邮箱和密码登录。

- 未验证邮箱：提示完成验证
- 已暂停账号：禁止登录
- 登录成功：进入 `/merchant/deals`
- 不提供普通用户登录

### 7.4 优惠码字段

商户新增优惠码只填写以下字段：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| 优惠码 | 是 | 用户在商户结账页输入的真实代码 |
| 优惠内容 | 是 | 例如“新用户首单 8 折” |
| 折扣类型 | 是 | `百分比折扣` 或 `固定金额折扣` |
| 折扣数值 | 是 | 例如 `20` 或 `15` |
| 使用限制 | 否 | 例如“仅限新用户，订单满 100 元” |
| 截止时间 | 否 | 为空表示长期有效 |

商户官网、商户名称和商户 Logo 不在每条优惠码中重复填写，自动从商户资料读取。

### 7.5 发布规则

- 商户保存优惠码后立即进入 `published`
- 不设置 `draft`
- 不设置 `pending_review`
- 不需要平台人工审核
- 商户只能编辑、暂停和恢复自己创建的优惠码
- 结束时间到达后自动变为 `expired`
- 商户资料暂停后，其全部优惠码不再公开展示

### 7.6 商户管理页面

页面只包含：

- 商户名称和官网
- 新增优惠码按钮
- 优惠码列表
- 编辑按钮
- 暂停/恢复按钮
- 当前状态
- 截止时间

不提供复杂统计、推荐权重、广告标记、批量导入和团队权限。

## 8. 数据模型

### 8.1 MerchantAccount

```text
id
email
password_hash
email_verified_at
verification_token_hash
verification_expires_at
status              # pending_verification / active / suspended
created_at
updated_at
```

### 8.2 Store

```text
id
owner_account_id
name
website_url
logo_url            # optional
status              # pending_verification / active / suspended
created_at
updated_at
```

### 8.3 PromoCode

```text
id
store_id
code
offer_text
deal_type           # percentage / fixed_amount
discount_value
terms
start_at
end_at
status              # published / paused / expired
created_at
updated_at
```

### 8.4 关系和约束

- 一个商户账号创建一个商户资料
- 一个商户资料拥有多条优惠码
- `MerchantAccount.owner_account_id = Store.owner_account_id`
- `PromoCode.store_id` 必须属于当前登录商户
- `code`、`offer_text`、`deal_type`、`discount_value` 必填
- `website_url` 必须是 HTTPS URL
- 同一商户的优惠码不允许完全重复

## 9. API 约定

### 9.1 公开接口

```text
GET  /api/promo-codes?q=&sort=&page=
POST /api/events/copy
GET  /go/:promoCodeId
```

`/go/:promoCodeId` 只记录点击并跳转到商户官网，不携带联盟参数。

### 9.2 商户认证接口

```text
POST /api/merchant/register
POST /api/merchant/login
POST /api/merchant/verify-email
POST /api/merchant/resend-verification
POST /api/merchant/logout
```

### 9.3 商户业务接口

```text
GET    /api/merchant/store
PATCH  /api/merchant/store
GET    /api/merchant/promo-codes
POST   /api/merchant/promo-codes
PATCH  /api/merchant/promo-codes/:id
POST   /api/merchant/promo-codes/:id/pause
POST   /api/merchant/promo-codes/:id/resume
```

所有商户接口都必须验证登录状态和资源归属。

## 10. 前端开发约定

### 10.1 当前实现

当前仓库使用前后端分离的生产实现：

- React
- Vite
- `lucide-react`
- Node.js API
- PostgreSQL
- 服务端 Session 和 HttpOnly Cookie
- SMTP 真实邮箱验证
- 所有业务数据由后端 API 管理

前端页面结构和交互保持轻量 MVP 设计，业务数据不落在浏览器存储中。

### 10.2 初始数据

生产环境不内置演示优惠码；数据库为空时，公开目录显示空状态，首批内容由已验证商户自行发布。

### 10.3 交互细节

- 复制按钮和商户入口使用图标加文字
- 所有可点击控件最小触控区域为 `44px`
- 复制成功使用 Toast 或内联状态提示
- 表格加载、空状态、错误状态必须明确
- 所有输入框有可访问名称
- 不用颜色作为唯一状态表达

## 11. 安全边界

- 密码只能保存哈希值
- 邮箱验证 token 只保存哈希值
- 商户只能访问自己的商户和优惠码
- 官网地址只允许 HTTPS
- 跳转目标只使用商户资料中的官网地址
- 注册和优惠码发布需要基础限流
- 同一商户优惠码重复提交时拒绝保存
- 商户可以暂停自己的优惠码
- 生产环境需要管理员具备紧急停用能力，但不作为本 MVP 页面

## 12. 指标

只记录最小必要事件：

- 优惠码曝光
- 优惠码复制成功/失败
- 商户官网跳转
- 商户注册
- 邮箱验证成功
- 优惠码发布

不记录真实订单、佣金、购买金额和个人用户画像。

## 13. 验收标准

### 13.1 公开目录

- 打开 `/` 可以看到中文优惠码列表
- 页面按“工具条、导航、Hero、表格”顺序展示
- 搜索可以过滤商户名称、优惠内容和优惠码
- 排序可以切换推荐、最新添加、即将结束
- 已过期和暂停优惠码默认不可见
- 点击优惠码可以复制正确文本
- 点击优惠码后可以打开对应商户官网
- 复制失败时仍有可读的优惠码和错误提示
- 无结果时显示明确空状态

### 13.2 商户

- 商户可以自行注册并填写商户名称和官网
- 未验证邮箱不能发布优惠码
- 邮箱验证成功后可以登录
- 商户可以新增优惠码并立即在公开目录看到
- 商户可以编辑自己的优惠码
- 商户可以暂停和恢复自己的优惠码
- 商户不能看到或修改其他商户的优惠码
- 商户官网地址不能被优惠码覆盖为任意外部地址

### 13.3 响应式和视觉

- 桌面端表格不产生横向滚动
- 移动端表格转换为单列卡片
- 关键操作按钮不小于 `44px`
- 页面使用深色背景、深紫色 Hero 和紫红色主色
- 页面不出现广告、订阅、语言、货币、主题切换入口
- `npm run build` 成功

## 14. 开发顺序

### Phase 1：公开目录

- 创建 Vite React 项目
- 完成全局视觉变量
- 完成工具条、导航、Hero 和表格
- 加入演示优惠码数据
- 实现搜索、排序、复制和跳转

### Phase 2：商户入口和服务端

- 完成商户注册和登录页面
- 完成真实邮箱验证
- 完成商户资料创建
- 完成优惠码新增、编辑、暂停和恢复
- 使用 PostgreSQL 保存商户和优惠码数据
- 使用服务端 Session 维护登录状态

### Phase 3：验证

- 桌面端和移动端检查
- 检查复制、跳转、搜索和表单流程
- 执行 `npm run build`
- 修复控制台错误和布局溢出
