# promo-code 管理后台 PRD

## 1. 产品定位

管理后台是 `promo-code` 的运营和风险控制中心。

平台采用“创建优惠后立即上线”的规则，后台不参与发布前审核，主要负责：

- 查看平台运行状态和核心数据
- 管理全部发布者资料和优惠码
- 处理失效、违规、虚假或恶意内容
- 暂停发布者并停止其优惠码展示
- 留存管理员操作记录

后台与公开目录、商户中心完全分离：

- 公开目录：普通用户浏览、搜索、复制优惠码
- 用户中心：用户管理收藏、举报和自己创建的优惠码
- 管理后台：管理员管理平台全部内容和发布者资料

## 2. 目标用户

### 2.1 管理员

管理员可以：

- 登录管理后台
- 查看平台概览
- 查看、搜索和筛选全部优惠码
- 下架、恢复和删除优惠码
- 查看商户资料和商户优惠码
- 暂停和恢复商户
- 查看操作日志
- 修改自己的管理密码

### 2.2 普通用户

普通用户不能访问管理后台。浏览、搜索、复制和跳转官网不需要注册或登录；收藏优惠码、提交举报和进入用户中心时需要普通用户登录。

### 2.3 用户创建优惠

用户不能访问管理后台，只能通过用户账号访问：

- `/user/register`
- `/user/login`
- `/user/verify`
- `/user/center`
- `/create-deal`

## 3. MVP 范围

### 3.1 必须实现

- 管理员登录和退出
- 管理后台独立布局
- 左侧导航和顶部工具栏
- 数据概览
- 优惠码管理
- 商户管理
- 举报管理
- 优惠码下架、恢复、删除
- 商户暂停、恢复
- 操作日志
- 管理员密码修改
- 管理后台响应式布局
- 空状态、筛选状态和无权限状态

### 3.2 暂不实现

- 优惠码发布前审核
- 普通用户管理
- 广告位管理
- 联盟佣金归因
- 订单、支付和订阅
- 多语言和货币切换
- 复杂角色权限
- 自动采集和第三方优惠码 API
- 复杂风控评分
- 用户历史记录

## 4. 路由设计

| 路由 | 页面 | 权限 |
| --- | --- | --- |
| `/admin/login` | 管理员登录 | 公开 |
| `/admin` | 数据概览 | 管理员 |
| `/admin/promo-codes` | 优惠码管理 | 管理员 |
| `/admin/merchants` | 商户管理 | 管理员 |
| `/admin/merchants/:id` | 商户详情 | 管理员 |
| `/admin/reports` | 举报管理 | 管理员 |
| `/admin/audit-logs` | 操作日志 | 管理员 |
| `/admin/settings` | 系统设置 | 管理员 |

未登录访问受保护路由时，跳转到 `/admin/login`。

后台入口不显示在公开前台导航中。

## 5. 整体布局

### 5.1 桌面端

- 左侧固定导航：宽度 `224px`
- 顶部工具栏：高度约 `64px`
- 主内容区：浅灰背景，最大宽度约 `1440px`
- 页面内容采用白色面板、细边框和紧凑间距
- 表格页面允许横向滚动

左侧导航：

```text
promo-code

数据概览
优惠码管理
商户管理
举报管理
操作日志
系统设置
```

底部显示当前管理员邮箱和退出按钮。

顶部工具栏：

- 当前页面标题和面包屑
- 刷新按钮
- 打开前台
- 管理员菜单

### 5.2 移动端

- 左侧导航折叠为顶部菜单按钮
- 主内容区占满屏幕
- 数据卡片改为单列或两列
- 表格容器允许横向滚动
- 删除、下架、暂停等危险操作仍保持可见且易点击

## 6. 数据概览

### 6.1 首屏指标

显示四个指标：

1. 当前在线优惠码
2. 已验证商户
3. 近 7 天新增优惠码
4. 近 7 天优惠码复制次数

如果复制事件还没有接入真实统计，显示“暂无数据”，不得使用虚假统计值。

### 6.2 辅助区域

- 近 7 天发布趋势
- 最近发布的优惠码
- 即将过期的优惠码
- 需要处理的异常内容
- 系统状态

概览页的作用是帮助管理员在 30 秒内判断平台是否正常，不做营销型数据大屏。

## 7. 优惠码管理

### 7.1 筛选条件

- 关键词：优惠码、优惠内容、商户名称、商户域名
- 状态：全部、已发布、已暂停、已过期、已下架
- 商户
- 发布时间
- 每页数量

### 7.2 表格字段

| 字段 | 说明 |
| --- | --- |
| 优惠码 | 真实优惠码 |
| 优惠内容 | 商户提交的描述 |
| 商户 | 商户名称和域名 |
| 状态 | 当前公开状态 |
| 发布时间 | 创建时间 |
| 截止时间 | 有效期 |
| 复制次数 | 匿名聚合统计，可选 |
| 操作 | 查看、下架、恢复、删除 |

### 7.3 操作规则

- 创建优惠后直接进入公开目录
- 管理员可以事后下架
- 下架必须填写原因或使用默认原因
- 删除必须二次确认
- 下架、恢复、删除写入操作日志
- 下架后的优惠码不出现在公开目录
- 恢复后的优惠码仍需满足有效期和商户状态条件

## 8. 商户管理

### 8.1 筛选条件

- 商户名称
- 邮箱
- 官网域名
- 邮箱验证状态
- 商户状态

### 8.2 表格字段

| 字段 | 说明 |
| --- | --- |
| 商户名称 | 展示名称 |
| 注册邮箱 | 登录邮箱 |
| 官网 | 商户官网 |
| 邮箱状态 | 已验证或未验证 |
| 优惠码数量 | 当前创建数量 |
| 商户状态 | 正常或已暂停 |
| 注册时间 | 创建时间 |
| 最近活动 | 最近发布或修改时间 |
| 操作 | 查看、暂停、恢复 |

### 8.3 商户暂停

商户暂停后：

- 商户不能登录或发布新的优惠码
- 该商户所有优惠码停止公开展示
- 原有数据保留
- 恢复商户后，符合有效期的优惠码恢复展示
- 暂停和恢复写入操作日志

## 9. 操作日志

记录以下操作：

- 管理员登录和退出
- 下架优惠码
- 恢复优惠码
- 删除优惠码
- 暂停商户
- 恢复商户
- 修改系统设置

字段：

- 时间
- 管理员
- 操作类型
- 目标对象
- 操作说明
- 操作结果

## 10. 系统设置

MVP 只包含：

- 修改管理员密码
- 是否允许新用户注册
- 公开站点状态
- 创建优惠限制：单商户累计优惠码上限、公开展示上限、每日新增上限、是否禁止同商户重复优惠码

暂不提供复杂角色权限。

当前实现使用密码哈希、数据库 Session 和 HttpOnly Cookie，前端不保存账号、密码或登录 token。

## 11. 数据模型

### 11.1 AdminAccount

```text
id
email
password_hash
status              # active / suspended
created_at
updated_at
last_login_at
```

### 11.2 AuditLog

```text
id
admin_id
action
target_type
target_id
description
created_at
```

### 11.3 PromoCode 扩展字段

```text
adminStatus         # normal / removed
adminRemovalReason
adminRemovedAt
adminRemovedBy
copyCount           # optional
```

### 11.4 MerchantAccount 扩展字段

```text
adminStatus         # normal / suspended
adminSuspendedAt
adminSuspendedBy
```

## 12. 权限和安全边界

- 管理后台必须独立认证
- 商户身份不能访问 `/admin/*`
- 管理员操作必须经过服务端授权
- 删除、下架、暂停等操作需要明确确认
- 正式环境不能在前端保存管理员明文密码
- 正式环境使用密码哈希、HttpOnly 会话和过期处理
- 后台操作需要保留日志
- 管理后台不泄露普通用户个人信息

## 13. 正式版 API 预留

```text
POST   /api/admin/login
POST   /api/admin/logout
GET    /api/admin/me
GET    /api/admin/dashboard
GET    /api/admin/promo-codes
POST   /api/admin/promo-codes/:id/remove
POST   /api/admin/promo-codes/:id/restore
DELETE /api/admin/promo-codes/:id
GET    /api/admin/merchants
GET    /api/admin/merchants/:id
POST   /api/admin/merchants/:id/suspend
POST   /api/admin/merchants/:id/resume
GET    /api/admin/audit-logs
PATCH  /api/admin/settings
GET    /api/admin/mail-settings
PUT    /api/admin/mail-settings
POST   /api/admin/mail-settings/test
```

邮件配置接口必须使用管理员会话授权。`GET` 返回当前配置时不得返回 SMTP 明文密码；密码字段为空时，`PUT` 保持原密码不变。`PUT` 请求体约定如下：

```json
{
  "fromAddress": "no-reply@example.com",
  "contentFormat": "multipart",
  "driver": "smtp",
  "smtp": {
    "host": "smtp.example.com",
    "port": 587,
    "encryption": "tls",
    "username": "no-reply@example.com",
    "password": "optional-on-update",
    "verifySsl": true
  },
  "testRecipient": "admin@example.com"
}
```

`POST /api/admin/mail-settings/test` 接收可选的 `recipient` 字段，服务端使用已保存的邮件配置发送测试邮件，并记录管理员操作日志。

## 14. 当前前端验收

- `/admin/login` 可以登录管理员
- 未登录不能进入 `/admin`
- `/admin` 显示概览数据
- `/admin/promo-codes` 可以搜索和筛选优惠码
- 可以下架、恢复和删除优惠码
- `/admin/merchants` 可以查看商户
- 可以暂停和恢复商户
- `/admin/reports` 可以筛选并处理用户举报
- 操作会产生日志
- 退出登录后重新进入后台需要登录
- 公开目录会同步反映下架优惠码和暂停商户
- `npm run build` 成功

## 15. 举报管理

举报由已登录的普通用户提交，公开目录中的每条优惠码提供“收藏”和“举报”两个操作入口。未登录用户点击操作时先跳转普通用户登录页；举报提交后不阻断优惠码继续展示，也不参与发布前审核。

后台 `/admin/reports` 提供：

- 按优惠码、商户名称、举报原因和用户邮箱搜索
- 按“待处理、已处理、已忽略”筛选
- 查看举报时间、优惠码、商户、举报原因、用户邮箱和当前状态
- 将待处理举报标记为已处理或忽略
- 所有处理动作写入后台操作日志

举报状态：

```text
pending    待处理
resolved   已处理
dismissed  已忽略
```

## 16. 商户资料录入规则

用户注册只填写邮箱和密码，并完成邮箱验证。商户名称和官网地址不在注册页填写，统一放入 `/create-deal` 的优惠码发布表单，表单字段顺序如下：

```text
商户名称
官网地址
优惠码
优惠内容
折扣类型
折扣数值
截止时间
使用限制
```

发布时必须满足：

- 商户名称不为空
- 官网地址必须为 HTTPS
- 官网地址不能命中后台网站黑名单
- 优惠码、优惠内容和折扣数值不为空
- 同一商户不能重复发布相同优惠码

发布成功后，商户名称和官网地址同步保存到商户资料，并立即用于公开目录展示。现阶段没有人工审核流程，创建优惠后直接上线；管理员只在收到举报或发现风险内容后进行事后处理。

## 17. 服务端数据边界

当前实现已经使用 PostgreSQL 保存商户、普通用户、优惠码、收藏、举报、网站过滤、管理员和审计日志；邮箱验证通过真实邮件服务发送，前端只通过 API 读取和修改数据。

当前接口：

```text
GET  /api/admin/reports
POST /api/admin/reports/:id/resolve
POST /api/admin/reports/:id/dismiss
```

## 18. 网站过滤规则

网站过滤支持两类规则：

### 18.1 域名规则

输入完整域名或官网地址，例如：

```text
https://t.me/
```

系统会规范化为主域名 `t.me`，并拦截：

```text
https://t.me/doingfb_group
https://t.me/another_path
https://sub.t.me/example
```

域名规则不会匹配相似但不同的域名，例如 `t.me.example.com`。

### 18.2 敏感词规则

输入不带完整域名的敏感词，例如：

```text
doingfb
```

系统会对官网 URL 的域名、路径和参数进行不区分大小写的包含匹配。因此包含该词的地址都会被拦截。

敏感词规则可能产生误拦截，运营人员应优先使用完整域名；只有需要跨域名拦截时再使用敏感词。

命中过滤规则后：

- 阻止用户注册或发布该官网
- 隐藏已经发布的相关优惠码
- 规则停用或删除后，符合其他条件的内容可以恢复展示
