FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev

COPY backend ./backend
RUN mkdir -p /app/backend/.data

ENV NODE_ENV=production
ENV API_PORT=8000

EXPOSE 8000

CMD ["node", "backend/server.mjs"]
